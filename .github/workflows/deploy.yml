name: implantando-no-servidor
run-name: Implantando o código no servidor.
on: [push]
jobs:
  implantando-no-servidor:
    runs-on: ubuntu-latest
    steps:
#########################################
#       - name: Install SSH Key
#         uses: shimataro/ssh-key-action@v2
#         with:
#           key: ${{ secrets.SSH_PRIVATE_KEY }}
#           known_hosts: 'just-a-placeholder-so-we-dont-get-errors'
#       - name: Adding Known Hosts
#         run: ssh-keyscan -p 22221 ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
#       - name: Enviar os arquivos para o destino
#         # run: rsync -avz ./dist/ ${{ secrets.SSH_USER}}@${{ secrets.SSH_HOST}}:~/teste/ -e 'ssh -p ${{ secrets.SSH_PORT}}'
#         run: ls -la ./

#########################################
#      - name: Deploy to Staging server
#        uses: easingthemes/ssh-deploy@main
#        with:
#          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
#          ARGS: "-rlgoDzvc -i"
#          SOURCE: "/home/runner/work/teste-ci-cd/"
#          REMOTE_HOST: ${{ secrets.SSH_HOST }}
#          REMOTE_USER: ${{ secrets.SSH_USER }}
#          REMOTE_PORT: ${{ secrets.SSH_PORT}}
#          TARGET: ${{ secrets.SSH_TARGET }}
#          EXCLUDE: "/dist/, /node_modules/"
#          SCRIPT_BEFORE: |
#            whoami
#            cd ${{ secrets.SSH_TARGET}}
#            ls -al
#          SCRIPT_AFTER: |
#            whoami
#            cd ${{ secrets.SSH_TARGET}}
#            ls -al
#            echo $RSYNC_STDOUT
      - name: Executa o comando SSH para entrar e fazer o que for necessário
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd ${{ secrets.SSH_TARGET}}
            git pull